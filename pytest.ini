[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# The integration is imported as custom_components.ica_shopping_list, which only
# resolves if the repo root is on sys.path — conftest.py lives in tests/, so
# pytest would otherwise add only that.
pythonpath = .
testpaths = tests
