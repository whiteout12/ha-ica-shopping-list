# Unpinned on purpose: this plugin ships a specific Home Assistant with it, and
# pinning an old one left Home Assistant's own shutdown thread running past the
# cleanup check — a failure that said nothing about this integration. Tracking
# the current release keeps the tests honest about the HA people actually run.
pytest-homeassistant-custom-component
